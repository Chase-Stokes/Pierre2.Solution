@{
  Layout = "_Layout";
}

@using Bakery.Models;

<h1>Flavors:</h1>

@if (@Model.Count == 0)
{
  <h3>No flavors have been added yet!</h3>
} 

<ul>
  @foreach(Flavor f in Model)
  {
    <li>@Html.ActionLink($"{f.Description}", "Details", new { id = f.FlavorId })</li>
  }
</ul>